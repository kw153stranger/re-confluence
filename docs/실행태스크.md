# Confluence AI 재구성 — 실행 태스크 (Execution Tasks)

> 구현 착수용 마일스톤별 체크리스트. 각 태스크는 `- [ ]`로 관리하고, 마일스톤마다 **DoD(수용기준)** 를 둔다.
> 기술 세부는 [`docs/구현설계.md`](구현설계.md), 배경은 [`docs/기획서.md`](기획서.md) 참고.
> 표기: `⟶ 설계 §x` = 구현설계서 해당 절, `⇒ 산출물` = vault 결과물.

## 마일스톤 개요

| 마일스톤 | 목표 | 선행 | 핵심 산출물 |
| --- | --- | --- | --- |
| **M0** 부트스트랩 | 개발 기반 마련 | — | 리포 구조·CLI 뼈대·CI |
| **M1** 기획·목업 | ✅ 완료 | — | `기획서.md`, `mockups/*` |
| **M2** Export→Analyze→Cluster | 수집·분석·군집 PoC | M0 | `raw/`, `analysis/`, `clusters.json` |
| **M3** Build + Review | IA 생성·사람 검수 | M2 | `build/`, `review.json` |
| **M4** Upload | 신규 Space 반영 | M3 | 신규 Confluence Space, `upload.log` |
| **M5** 하드닝 | 성능·운영·문서화 | M4 | 배치 재수집·성능 개선·백로그 |
| **M6** 웹서비스 + 벡터DB | 코어 재사용 웹서비스·검색 | M3 | FastAPI·pgvector·워커·프런트 |

---

## M0 — 부트스트랩 (Bootstrap) ✅ 완료

**에픽: 프로젝트 기반**
- [x] `pyproject.toml` 작성 (Python 3.11+, 의존성: typer·pydantic·httpx·rapidfuzz 등) ⟶ 설계 §2
- [x] `src/reconf/` 패키지 레이아웃 생성 ⟶ 설계 §3
- [x] `typer` CLI 진입점(`cli.py`) — 6개 서브커맨드 스텁 + 공통 옵션(`--vault/--config/--resume/--dry-run`) ⟶ 설계 §5
- [x] `config.py` 설정 로더 + `config.example.yaml` ⟶ 설계 §7
- [x] `models.py` pydantic 모델 정의 (`AnalysisResult`·`Cluster`·`ReviewDecision`·`PageProperties` 등) ⟶ 설계 §4
- [x] `store.py` vault 경로 규칙·읽기/쓰기 유틸 + `content_hash` ⟶ 설계 §3·§12.6
- [x] 구조화 로깅 설정(`logging_setup.py`) ⟶ 설계 §9
- [x] `embed.py`·`labels.py` 인터페이스 스텁(캐시 조회·라벨 정규화) ⟶ 설계 §6.3·§6.4a

**에픽: 품질/CI**
- [x] `ruff`(lint/format) + `pytest` 설정
- [x] CI 워크플로우(lint + test) `.github/workflows/ci.yml`
- [x] `README` 개발 셋업 섹션(설치·실행) 갱신

**DoD (M0)** — ✅ 검증 완료
- [x] `reconf --help`가 6개 서브커맨드를 표시하고 각 스텁이 실행된다.
- [x] `pytest`(17 passed)·`ruff`(clean)가 통과한다.
- [x] pydantic 모델이 기획서 Analyze 스키마와 필드·타입이 일치한다(roundtrip 테스트).

---

## M2 — Export → Analyze → Cluster ✅ 로직 완료(라이브 스모크 잔여)

**에픽: Export** (`export.py`·`confluence.py`·`markdown.py`) ⟶ 설계 §6.1
- [x] Confluence 페이지 목록·본문·메타 수집 — `ConfluenceClient` 프로토콜 + `ConfluenceRestClient`(REST, 페이지네이션)
- [x] 첨부파일 **바이너리 다운로드** → `raw/attachments/<page_id>/`
- [x] HTML(storage)→Markdown 변환 + frontmatter 부착 ⟶ 기획서 §4.1
- [x] 멱등 저장(`source_page_id` 키, 수정일 변경 시에만 갱신) ⇒ `raw/*.md`

**에픽: Analyze** (`analyze.py`·`llm.py`·`prompts.py`) ⟶ 설계 §6.2·§8
- [x] `llm.py` OpenAI 호환 클라이언트(vLLM/Ollama 엔드포인트)
- [x] 분류·요약 프롬프트(system=스키마 강제) 작성
- [x] JSON 파싱·`AnalysisResult` 검증·실패 재요청(최대 N회)
- [x] 긴 문서 **청크 요약·통합**(`condense_body`) + `confidence` 산출 ⇒ `analysis/<page_id>.json`

**에픽: Cluster** (`cluster.py`·`embed.py`) ⟶ 설계 §6.3
- [x] `embed.py` TEI 클라이언트 + **임베딩 캐시**(`embeddings/<page_id>.json`, `content_hash` 키)
- [x] 신규·변경 문서만 임베딩(캐시 재사용), 배치 호출
- [x] `business`+`year` 그룹핑 (project 활용은 후속)
- [x] 제목 유사도(`rapidfuzz`) + 캐시된 본문 임베딩 코사인 유사도
- [x] 임계값 기반 중복 후보 표시 + 정본(canonical) 선정 ⇒ `clusters.json`

**DoD (M2)** — 🟡 fake 클라이언트로 검증 완료, 실 서비스 스모크는 잔여
- [x] `export→analyze→cluster` 체이닝이 오류 없이 완료된다(E2E fake 통과).
- [x] `analysis/*.json` 전수가 `AnalysisResult` 스키마 검증을 통과한다.
- [x] 임베딩이 `embeddings/*.json`에 저장되고, 재실행 시 재계산 없이 재사용된다.
- [x] 중복 문서 쌍이 `clusters.json`에서 중복 후보로 잡히고 정본이 1개 지정된다.
- [x] 각 단계 로직이 단위/통합 테스트로 검증된다(pytest).
- [ ] **실 서비스 스모크**: 사내 Confluence·Qwen3·TEI 대상 1회 실행 검증(환경 필요).

> 이 샌드박스에는 실 서비스(mcp-atlassian/LLM/TEI)가 없어, 동일 프로토콜의 fake로 E2E를 검증했다.
> 실 환경에서는 환경변수(CONFLUENCE_*)·엔드포인트만 설정하면 동일 코드가 동작한다.

---

## M3 — Build + Review ✅ 로직 완료

**에픽: 라벨 마스터** (`labels.py`) ⟶ 설계 §6.4a
- [x] 라벨 레지스트리(`labels.json`, `LabelRegistry`) 로드/저장
- [x] `resolve_label`: 정규화 → 정확매칭 → `rapidfuzz` 유사매칭으로 canonical 통합(alias 흡수)
      (임베딩 유사매칭은 `embed_sim` 콜백 옵션으로 주입 가능)
- [x] 미매칭 라벨은 `candidate`로 등록, 라벨별 `count` 집계

**에픽: Build** (`build.py`) ⟶ 설계 §6.4
- [x] 업무→연도 Page Tree 생성 + `build/tree.json`(순서·부모관계) + `build/pages.json`
- [x] 문서별 Page Properties 블록·표준 필드 생성 ⟶ 기획서 §5.3
- [x] 라벨을 **마스터 canonical로 정규화**해 부여(`업무/`·`시스템/`·`연도/`·`상태/`) ⇒ `build/*.md`
- [ ] 업무별 Summary(index) 페이지 + Page Properties **Report** 매크로 (tree엔 index 노드 존재, 매크로 렌더는 Upload에서)

**에픽: Review** (`review.py`) ⟶ 설계 §6.5
- [x] 검수 큐 export(`--export-queue`): 저신뢰·중복후보 우선 정렬 ⇒ `review_queue.json`
- [x] 사람 수정 반영(`--apply <file>`), 승인/반려 상태 관리 ⇒ `review.json`
- [x] **라벨 후보(candidate) 승인** 처리 ⇒ `labels.json` 반영
- [x] 승인 게이트: 승인 문서만 Upload 대상에 포함(Upload에서 필터)
- [ ] 목업 `mockups/pipeline-dashboard.html` 검수 UI ↔ `review_queue.json` 실연동(웹은 M6)

**DoD (M3)** — ✅ 테스트 검증
- [x] `build/` 산출물이 IA(업무→연도)·라벨·Page Properties 표준을 따른다.
- [x] 모든 라벨이 마스터 canonical로 통일되어 유사중복 라벨이 생기지 않는다.
- [x] 검수 큐가 저신뢰·중복 후보를 상단에 노출하고, 라벨 후보를 승인할 수 있다.
- [x] `review.json` 승인 결과가 Upload 대상 필터에 반영된다.

---

## M4 — Upload ✅ 로직 완료(라이브 스모크 잔여)

**에픽: 신규 Space 반영** (`upload.py`·`confluence.py`) ⟶ 설계 §6.6
- [x] 신규 Space 생성(존재 시 재사용) — `ensure_space`
- [x] 페이지 upsert(`source_page_id` 멱등 키), 업무 index를 부모로
- [x] Labels·Summary·Properties 본문 반영
- [x] 원본 back-link 삽입(추적성)
- [x] 부분 실패 격리 + 실패 목록 ⇒ `upload.log` (지수 백오프 재시도는 후속)
- [ ] 첨부 업로드(현재 본문·라벨 우선; 첨부 전송은 후속)

**DoD (M4)** — 🟡 fake writer로 검증, 실 서비스 스모크 잔여
- [x] 검수 승인 문서만 신규 Space에 업무 그룹으로 생성된다(fake E2E).
- [x] 재실행 시 중복 페이지가 생기지 않는다(멱등 upsert 검증).
- [x] 부분 실패가 격리되고 `upload.log`에 기록된다.
- [ ] Page Properties Report / Content by Label 조회 동작(실 Space 필요).
- [ ] **실 서비스 스모크**: 사내 Confluence 대상 1회 업로드 검증(원본 Space 불변 확인 포함).

---

## M5 — 하드닝 (Hardening)

- [ ] 대량(수천 페이지) 처리 성능·병렬화·레이트리밋 튜닝
- [ ] 배치 재수집(증분 Export) 지원
- [ ] 운영 문서(Runbook)·트러블슈팅 정리
- [ ] (백로그) ACL·워크플로우 이관, 양방향 동기화 — 기획서 Non-goals, 별도 과제

**DoD (M5)**
- 목표 규모에서 전체 파이프라인이 재현 가능하게 완주한다.
- 운영자가 Runbook만으로 재수집·부분 재실행을 수행할 수 있다.

---

## M6 — 웹서비스 + 벡터DB (하이브리드) ✅ 로직 완료 ⟶ 설계 §12

> 파이프라인 코어(`src/reconf`)는 유지하고 그 위에 웹서비스를 얹는다. **사내망 셀프호스트 전제**.

**에픽: 백엔드/오케스트레이션** (`web/app.py`·`cli.py serve`) ⟶ 설계 §12.1·§12.3
- [x] FastAPI 스캐폴딩 + `reconf` 코어 재사용(중복 구현 없음), `reconf serve` 실행
- [x] 실행/상태 API(`POST /api/runs`, `GET /api/runs/{id}`) — 인라인 실행 + 인메모리 런 레지스트리
- [ ] 잡 큐(arq/RQ + Redis) 워커로 장시간 단계 비동기화(현재 인라인; 워커 위임은 후속)

**에픽: 벡터DB(pgvector)** (`vectorstore.py`·`migrations/001_init.sql`) ⟶ 설계 §12.2
- [x] Postgres + `pgvector` 스키마(`doc_embeddings` HNSW 인덱스) 마이그레이션 SQL
- [x] `VectorStore` 추상화: `FileVectorStore`(기본·테스트) + `PgVectorStore`(psycopg)
- [x] 의미 검색 API(`POST /api/search`): 쿼리 임베딩(TEI) → 코사인 근접 + `business/year` 필터
- [x] `config.db.backend`(`file|postgres`)로 백엔드 선택
- [ ] 임베딩·상태를 pgvector/테이블로 실적재(psycopg 라이브 검증은 Postgres 필요)

**에픽: 상태·거버넌스 API** (`web/app.py`) ⟶ 설계 §6.4a·§6.5·§12.3
- [x] 검수 큐 조회·승인/반려(`GET /api/review/queue`, `POST /api/review/decisions`)
- [x] 라벨 후보 승인/병합(`POST /api/labels/resolve`)

**에픽: 프런트** ⟶ 설계 §12.1
- [ ] 목업(`mockups/*`)을 실제 앱으로 승격(검수 UI·대시보드·검색) — API는 준비됨, JS 연동은 후속

**에픽: 배포·보안** ⟶ 설계 §12.4
- [ ] 사내망 셀프호스트 배포(프런트·API·워커·DB·LLM·TEI 동일망), 비유출 검증
- [ ] 인증(사내 SSO)·검수자 RBAC·감사 로그

**DoD (M6)** — 🟡 API·검색 라이브 검증, pgvector 실적재/프런트 잔여
- [x] 웹에서 파이프라인 단계를 실행하고 검수·라벨 거버넌스를 수행할 수 있다(TestClient + 실 uvicorn 스모크).
- [x] 의미 검색이 `business/year` 필터와 함께 동작한다(FileVectorStore).
- [x] CLI와 웹이 동일한 `reconf` 코어를 공유(로직 중복 없음)한다.
- [ ] pgvector 실적재·ANN 검색 라이브 검증(Postgres 필요).
- [ ] 프런트 실앱 연동, 잡 큐 워커, 사내망 배포·인증.

---

## 진행 추적

| 마일스톤 | 상태 | 비고 |
| --- | --- | --- |
| M0 | ✅ 완료 | 부트스트랩 (reconf CLI·모델·CI) |
| M1 | ✅ 완료 | 기획서·목업 |
| M2 | ✅ 로직 완료 | Export/Analyze/Cluster 구현+테스트 (라이브 스모크 잔여) |
| M3 | ✅ 로직 완료 | Build·Review·라벨마스터 구현+테스트 |
| M4 | ✅ 로직 완료 | Upload 멱등·승인필터·부분실패 (라이브 스모크 잔여) |
| M5 | ⬜ 예정 | 하드닝 |
| M6 | ✅ 로직 완료 | 웹서비스·벡터DB (pgvector 실적재·프런트 잔여) |
